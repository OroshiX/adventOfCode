package advent

import java.util.*

fun main(args: Array<String>) {
    val scanner = Scanner(System.`in`)
    solve20(scanner)
}

fun solve20(scanner: Scanner) {
    with(scanner) {
        while (hasNext()) {
            val testNumbers = nextLine().toInt()
        }
        TODO()
    }
}